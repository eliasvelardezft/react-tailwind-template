import React from 'react';

const Form = () => {

    return(
        <>
            <form action="">
                <label htmlFor="email">Email</label>
                <input type="text" name="email" id="email"/>
            </form>
        </>
    );
};

export default Form;